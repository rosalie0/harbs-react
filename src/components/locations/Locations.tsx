import React from "react";
import SingleLocation from "./SingleLocation";

const Locations = () => {
  // Chelsea first.
  // Soho second.
  return (
    <div>
      <SingleLocation />
      <SingleLocation />
    </div>
  );
};

export default Locations;
